services:
  database:
    image: 'postgres:15.2'
    ports:
      - '5432:5432'
    environment:
      - 'POSTGRES_USER=sebi'
      - 'POSTGRES_DB=sebisprojectsdb'
      - 'POSTGRES_PASSWORD=sebisProjectsPass'

  libretranslate:
    image: libretranslate/libretranslate
    container_name: libretranslate
    restart: unless-stopped
    ports:
      - '5001:5000'
    volumes:
      - lt-local:/home/libretranslate/.local

volumes:
  lt-local: